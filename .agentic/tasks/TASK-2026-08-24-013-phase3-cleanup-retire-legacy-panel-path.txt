TASK: TASK-2026-08-24-013
CREATED: 2026-08-24
SOURCE: ui-div-layer-implementation.txt
PRIORITY: medium
STATUS: PENDING
DEPENDS_ON: TASK-2026-08-24-012
ESTIMATE: half-day (~3-4 hours)

## Title
Phase 3 (cleanup): retire the Jint UI host panel path, PanelFfi/LAST_PANELS, and superseded type contracts

## Description
Plan reference: plans/ui-div-layer.md, Phase 3 ("Packaging, lifecycle, cleanup"):
- ".ui-aware packaging (module discovery, manifest permissions, enable/disable)"
- "Module unload / hot-reload ... deregister a module's nodes, reconcile out."
- "Retire: duplicated Jint UI host (HostApiSetup.cs panel path), vestigial Rust
  PanelFfi/LAST_PANELS, suite/types/ui/Panel.d.ts, and the two todo/ type drafts (promote
  them into .ui/types)."
- "Supersede plans/simplify-ui-panel-api.md."

This task does the RETIREMENT + packaging/hot-reload. It is only safe after ALL modules are
migrated (tasks 007 + 008 complete) and Phase 2c is in (012) so nothing still depends on the
old panel pipeline.

Work items:
1. C# retirement (application/client/solution/Sources/):
   - Retire the Jint UI host panel path: HostApiSetup.cs's registerPanel + the __host_registerPanel
     bridge in ModuleEngine.cs, PanelCollector.cs, PanelParser.cs, ContentParser.cs,
     LayoutParser.cs, TemplateExecutor (if present), and the legacy renderers:
     Panel.cs (Godot.Panel-based), RootNode.cs, the 5 *ContentNode.cs classes + IContentNode.cs,
     Root.cs, RuntimeUi.cs (if vestigial). The new path (UiSceneReconciler + division/window/
     text/field/image/canvas renderers) becomes the ONLY UI path.
   - RuntimeInterop.cs: remove the per-frame polling wrappers that are now unreferenced
     (GetEntityTextMapValue / ReadEntityTextValue / GetEntityNumberMapValue /
     ReadEntityNumberValue + their DllImports) IF nothing else uses them — grep first; the
     Rust FFI exports themselves (src/ffi_mod/misc/get_entity_*_map_value.rs) stay for now
     (other consumers/tests may use them; removing Rust FFI exports is a separate, riskier
     step — document the decision).
   - Keep HoverOutline.cs + SpriteMapCpu.cs (still used by the new renderers).
2. Rust retirement (application/runtime/src/):
   - Remove the vestigial panel machinery: LAST_PANELS (src/state/mod.rs + accessors),
     PanelFfi / panel_types.rs / panel_conversions.rs (src/ffi_mod/types/), the panel
     entries in export_state_struct/state_collection.rs + free_exported_state.rs, and the
     `panels` field in the Declarations struct (src/js_host_api/mod.rs) + its extraction in
     script_extract.rs — ONLY after confirming no test/feature still asserts on panels (grep
     the restricted dirs is allowed for READING; they must not be modified — if a restricted
     test depends on LAST_PANELS/panel export, STOP and flag it as a blocker note instead of
     deleting).
   - The old registerPanel host stub in script_panel_entity.rs becomes fully vestigial —
     remove it (modules no longer call it after migration).
3. Types promotion + retirement:
   - Promote specification/types/todo/user-interface/*.ts (division.ts, panel.ts, text.ts)
     into application/ui/types/ as the canonical .ui type contracts (merge with what tasks
     003/004/010 already created — the .ui/types files win on conflict; the todo/ drafts are
     then deleted).
   - Delete application/suite/types/ui/Panel.d.ts (+ index.d.ts reference + the
     RegisterPanelFunction import in suite/types/HostApi.d.ts) — and replace with a
     re-export/import of the canonical application/ui/types contracts if suite TS typecheck
     needs a local copy (suite tsconfig includes its own types/; check
     application/suite/tsconfig.json and TS-TSC-CHECK.md first, pick the minimal-change
     wiring, keep `tsc --noEmit` green in the suite Maven build).
   - Note: application/suite/src/test/resources/features/ is restricted — the stage2
     panel_initialization feature there (offset/index.js uses registerPanel) is a JAVA-side
     fixture; it does NOT run against the C# host (it is the Jint-independent declaration
     test). If it is purely declaration-level, it is unaffected by the C# retirement —
     verify by reading (allowed), do NOT edit it.
4. Packaging + lifecycle (.ui-aware):
   - Module discovery/manifest: the archive pipeline (src/module/manifest.rs) already
     discovers index.js + manifest.json. Add .ui awareness: manifest may declare `permissions`
     (minimal: a list of allowed ui actions / state slots — enforced by the host API: an
     unpermitted ui.action registration fails with a logged error) and enable/disable
     (manifest `"enabled": false` -> module skipped). Keep it small; document the v1 scope.
   - Hot-reload / unload: implement module UNLOAD — given a module id, deregister its UI
     nodes (all node ids it owns — the registry in application/ui/ui/registry.js tracks
     owner-module per node; mark them removed -> the next tick's delta removes them ->
     the reconciler frees the Godot nodes). Full hot-swap under quiescence is staged: v1 =
     unload + re-load (a "reload module X" FFI/debug command that re-runs its entrypoint).
     Wire a debug entry point (src/ffi_mod/debug/) for testing.
5. Supersede the old plan: mark plans/simplify-ui-panel-api.md as superseded (add a
   "SUPERSEDED by plans/ui-div-layer.md" header line — plan files are docs, safe to touch).
6. Final verification (the plan's Verification item 5):
   - application/suite Maven build (incl. tsc --noEmit) passes.
   - C# dotnet build + the full GdUnit4 stage suite passes (run via cargo test which drives
     mvn + dotnet test — test/java_tests.rs).
   - Existing test modules migrated, not deleted (task 008's guarantee — re-verify counts).

Constraints:
- Do NOT edit the three restricted directories. If any of them still references retired
  artifacts (registerPanel, panel FFI), do NOT force the retirement — leave the artifact,
  record a blocker note in the commit message, and narrow the retirement scope.
- Git discipline per .agentic/README.txt: file moves as separate commits (create dir ->
  commit, move -> commit, update -> commit, delete old -> commit); conventional commits
  referencing TASK-2026-08-24-013.
- This task should be splittable: if 3-4 h runs short, land the C#+Rust retirement first
  (items 1-2), then types (3), then packaging/hot-reload (4) — note which part shipped.

## Acceptance Criteria
- dotnet build passes; the new UI path is the only panel path (grep: registerPanel absent
  from Sources/; HostApiSetup.cs panel bridge, PanelCollector, PanelParser, ContentParser,
  LayoutParser, *ContentNode, legacy Panel.cs/RootNode.cs removed or clearly marked
  obsolete-and-unreferenced).
- cargo test passes (drives mvn + dotnet test): suite Maven build + tsc --noEmit green, full
  stage suite green with zero skipped tests.
- Rust: LAST_PANELS / PanelFfi removed (or blocker note recorded if a restricted consumer
  still needs them); cargo test still green.
- application/ui/types/ is the single canonical type contract (todo/ drafts promoted +
  deleted; suite/types/ui/Panel.d.ts gone or re-exporting .ui/types; suite tsc green).
- Hot-reload: debug unload of a module removes exactly its nodes from the live UI next tick
  (scripted check via the ManualTest scene or a cargo-level delta test).
- plans/simplify-ui-panel-api.md carries the superseded marker.
- git diff: no changes under the restricted directories.

## Dependencies
- Depends on TASK-2026-08-24-012 (all Phase 2/2b/2c work merged). Practically it also needs
  TASK-2026-08-24-008 (human stage-module migration) COMPLETE — do not retire the Jint panel
  path while stage modules still use registerPanel. If 008 is still pending when this task
  starts, work the non-retirement parts (packaging/hot-reload/types promotion) and leave the
  C# panel-path retirement as the final step gated on 008.
