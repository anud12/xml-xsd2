TASK: TASK-2026-08-24-007
CREATED: 2026-08-24
SOURCE: ui-div-layer-implementation.txt
PRIORITY: high
STATUS: PENDING
DEPENDS_ON: TASK-2026-08-24-006
ESTIMATE: half-day (~3-4 hours)

## Title
Phase 2 (migration): migrate MainModule to ui.div composition (delta-driven, no FFI polling)

## Description
Plan reference: plans/ui-div-layer.md, Phase 2 deliverable ("MainModule + Test/Stage_* UI
modules migrated to ui.div composition; identical visuals; C# no longer calls
get_entity_*_value per frame") and Verification item 3 ("migrate MainModule + Test/Stage_* UI
modules to ui.div; confirm visuals, hover, z-order; grep confirms C# no longer polls
get_entity_*_value").

Scope split: THIS task migrates MainModule only (it is the production demo and the real-time
verification subject). The Test/Stage_* module migrations are blocked for agents (restricted
directory) and are handled in task 008 as a specification for a human implementer.

MainModule today (application/client/solution/MainModule/index.js):
- entity "entity_id" with numberMap.key = 0, textMap.isModified = "No";
- effects: "key-modify-if-par" (sets isModified = "Yes" when key % 3 == 0) and "repeat"
  (reoccurring, increments key while key <= 20);
- registers animations "hover" + "texture" (frame_1..5.png);
- registerPanel "center": 100x100, offset 70 all sides, entityNumberValue content for "key"
  align top, hover (hover.png, thickness 5), animated background "texture" duration 5 loop;
- registerPanel "isModifiedPanel": 100x100, offset top 100 / bottom 100 / left 250 / right
  250, entityTextValue content for "isModified" align center, hover thickness 10, animated
  background "texture" duration 30 loop.

Work items:
1. Rewrite MainModule/index.js in the new API (the module itself is NOT in a restricted
   directory — only Test/ is restricted):
   - Keep the entity + both effects EXACTLY as they are (simulation semantics unchanged).
   - Keep the registerAnimation calls (the animation registry is shared by both paths).
   - Replace the two registerPanel calls with:
       ui.window("main", { position/size matching the current panels' placement, clip ... },
         [
           ui.div("center", {
             x/y (or window-relative placement) reproducing offset 70-all-sides, size 100x100,
             background: { name: "texture", duration: 5, loop: true },
             hover: { texture: <hover anim ref>, thickness: 5 },
             onClick: { type: "emitAction", actionName: "increment" } (preserve existing
             behavior if the panel had onClick; today MainModule's panels have no onClick —
             keep it that way),
           }, [
             ui.field("center-value", "entity_id", "number", "key", { fallback: "" })
             (align: top — express via the field/div options available after task 003-004;
             if per-leaf align is not exposed yet, add a minimal align option to the text/field
             leaf in application/ui + types + Rust node model in this task, with tests),
           ]),
           ui.div("isModifiedPanel", { ... offset top 100/bottom 100/left 250/right 250,
             100x100, background: { name: "texture", duration: 30, loop: true },
             hover: { texture: <hover anim ref>, thickness: 10 } },
             [ ui.field("isModified-value", "entity_id", "text", "isModified", { fallback: "" })
               (align: center) ]),
         ])
     NOTE on offset semantics: the legacy Panel offset model (top/bottom/left/right from
     screen edges) maps to window/coordinate-escape placement — translate the concrete pixel
     positions, keep the on-screen result identical (that is the "identical visuals" bar).
2. Verify the entity-value path: center-value must update every tick while "repeat" is active
   (key increments to 20), and isModified-value flips to "Yes" on the ticks where key % 3 == 0
   (key=3,6,9,12,15,18 — and 0 at start).
3. If the align gap in task 003-005 blocks "identical visuals", close it here (leaf align
   option end-to-end: .ui types + dag.js + Rust UiNode + C# renderer + tests). Keep it small.
4. C# (only if needed): ensure the Game scene loads the new-format UI for MainModule via the
   reconciler (task 006's gating). No changes to the legacy path.
5. Regression check: run the client with the migrated MainModule; compare visually against the
   legacy render (two runs, screenshots or side-by-side): same panel positions, sizes,
   backgrounds animating at the same speeds, same hover behavior, same text values/alignment,
   z-order identical (declaration order).

Constraints:
- Do NOT edit the three restricted directories (this is why the Stage_* migrations are a
  separate human task, 008).
- Do not change simulation semantics: entity, effects, and animations stay behaviorally
  identical — only the UI declaration surface changes.
- After this task, when MainModule is loaded via the new path, C# must not poll
  get_entity_*_value for its panels (grep the new renderer; the legacy classes still contain
  such calls but are not on MainModule's path).

## Acceptance Criteria
- MainModule/index.js declares its UI exclusively with ui.window/ui.div/ui.field (+
  registerAnimation); no registerPanel call remains in the file.
- dotnet build passes; cargo test passes.
- Running the client: the two panels appear at the same screen positions/sizes as before,
  with animated backgrounds, hover outlines (thickness 5 and 10), and correct alignment
  (top / center).
- Real-time: center-value counts up every tick while key <= 20, then freezes; isModified-value
  shows "Yes" exactly when key % 3 == 0 (matches legacy behavior).
- Grep: no get_entity_*_value / Read*Entity*Value call is reachable from the new UI path for
  MainModule (document the grep command + result in the commit message).
- git diff: no changes under the restricted directories.

## Dependencies
- Depends on TASK-2026-08-24-006: needs the hardened reconciler + division/window/field
  renderers to host the migrated module.
