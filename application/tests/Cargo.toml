[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
cucumber = "0.20"
async-std = { version = "1.10", features = ["attributes"] }
futures = "0.3.32"
regex = "1.12.3"

[dev-dependencies]
