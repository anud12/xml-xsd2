# Stage - Behavior

## Purpose

Stage validates **behavior rule graphs** for entities: blackboard-driven priority/utility rules
whose step scripts (action and wait steps) are validated at module load time and executed across runtime iterations.
