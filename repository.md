# Repository API

## Purpose
A repository to query the enviroment
Repository has a function `get` which takes one of:
    - `EntityQuery`
    - `ContainerQuery`
    - `NumberMapQuery`
    - `TextMapQuery`